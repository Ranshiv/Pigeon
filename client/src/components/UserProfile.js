import React from 'react';

const UserProfile = () => {
    // Fetch user details here later
    return (
        <div>
            <h2>User Profile</h2>
            <p>User profile details and options will go here.</p>
        </div>
    );
};

export default UserProfile;